# Generation Report

Current provider modes:

- `mock`: returns fixed demo assets.
- `cache`: returns the first reviewed asset matching the requested kind.
- `ascend`: reserved for real NPU generation.

Track speed, success rate, resource usage and fallback cases here.
