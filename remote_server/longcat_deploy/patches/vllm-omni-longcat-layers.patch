diff --git a/vllm_omni/diffusion/models/longcat_image/longcat_image_transformer.py b/vllm_omni/diffusion/models/longcat_image/longcat_image_transformer.py
index 2f6d54a..fc1dcee 100644
--- a/vllm_omni/diffusion/models/longcat_image/longcat_image_transformer.py
+++ b/vllm_omni/diffusion/models/longcat_image/longcat_image_transformer.py
@@ -519,8 +519,12 @@ class LongCatImageTransformer2DModel(nn.Module):
         od_config: OmniDiffusionConfig,
         patch_size: int = 1,
         in_channels: int = 64,
-        num_layers: int = 19,
-        num_single_layers: int = 38,
+        # LongCat-Image's pinned checkpoint declares 10 dual-stream blocks and
+        # 20 single-stream blocks in transformer/config.json.  The previous
+        # Flux-derived 19/38 defaults created extra randomly initialized layers
+        # because the custom Omni pipeline does not pass the component config.
+        num_layers: int = 10,
+        num_single_layers: int = 20,
         attention_head_dim: int = 128,
         num_attention_heads: int = 24,
         joint_attention_dim: int = 3584,

