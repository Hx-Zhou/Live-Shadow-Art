# 光影新生 · 项目分工与框架详细说明

> 本文档用于明确 A/B/C 三名成员的目录责任、协作边界、工程框架和各模块职责。
> 当前仓库阶段：已有项目计划书与框架说明，源码目录尚未初始化。后续搭建工程时建议严格按本文档划分所有权，避免多人频繁修改同一模块。

## 一、项目当前要做什么

“光影新生”是一套 AI 实时互动皮影戏台。

项目目标不是让 AI 自动生成一段完整视频，而是建立一条可交互的数字皮影生产与操控链路：

```text
选择角色与场景风格
  -> 组合贴图提示词
  -> 昇腾 NPU 生成角色/场景贴图
  -> 贴图后处理与角色包导出
  -> 数字皮影骨架绑定
  -> 摄像头手势识别
  -> 连续关节映射与动作触发
  -> 用户自由操控、切换背景、录制回放
```

首版 MVP 需要在 1 周内完成一个可演示闭环：

- 至少 2 个可自由操控角色；
- 至少 3 个可即时切换背景；
- 至少 2 套骨架模板：人形、动物形；
- 至少 8 个基础动作：待机、移动、转身、抬手、行走、跳跃、攻击、受击；
- 支持单手实时控制和至少 5 类可靠手势；
- 支持贴图配置、候选选择、校准、自由操控、录制回放；
- 支持 `mock`、`cache`、`ascend` 三种运行模式；
- 即使昇腾生成或摄像头识别临时不可用，也能通过缓存资产和键鼠 Mock 完成现场演示。

## 二、总体架构

项目分为三条主线：

```text
B 线：手势识别                         C 线：舞台与控制                         A 线：昇腾生成

摄像头 / MediaPipe / 小模型       ->     React + PixiJS + 控制状态机       <-     Stable Diffusion / LoRA
连续信号 + 离散手势事件                  角色骨架 / 动作库 / UI / 录制             角色与场景贴图生成
```

三条主线通过三个边界协作：

```text
边界 1：GestureSource.ts
B 线输出连续控制信号和离散手势事件，C 线只消费统一接口。

边界 2：openapi.yaml
C 线前端/后端通过 REST 和 WebSocket 调用 A 线生成服务。

边界 3：角色包资产格式
A/C 都遵守“部件 PNG + rig.json + manifest.json”的资产包格式。
```

系统运行时可按环境变量切换生成能力：

```text
PROVIDER=mock     # 返回固定 demo 角色包，适合前端和手势独立开发
PROVIDER=cache    # 使用已生成并审核过的缓存资产，适合联调和答辩
PROVIDER=ascend   # 连接昇腾 NPU 真实生成服务
```

## 三、推荐工程目录

后续初始化项目时建议采用以下结构：

```text
project/
├── package.json
├── pnpm-workspace.yaml
├── packages/
│   └── schemas/
│       ├── gesture-event.schema.json
│       ├── continuous-signal.schema.json
│       ├── rig.schema.json
│       ├── manifest.schema.json
│       ├── script-act.schema.json
│       └── openapi.yaml
│
├── web/
│   └── src/
│       ├── gesture/
│       ├── control/
│       ├── theater/
│       ├── assets/
│       ├── script/
│       ├── recording/
│       └── ui/
│
├── server/
│   └── app/
│       ├── api/
│       ├── generation/
│       ├── postprocess/
│       └── assets/
│
├── assets/
│   ├── rigs/
│   ├── motions/
│   ├── textures/
│   ├── demo/
│   ├── characters/
│   └── backgrounds/
│
├── controls/
│   └── controls.json
│
├── models/
│   ├── generation/
│   └── gesture/
│
├── prompts/
├── tests/
└── docs/
```

## 四、成员 A 职责：昇腾生成线

成员 A 负责图像生成模型部署、推理服务、模型配置、缓存与失败回退。

### A 主责目录

```text
server/app/generation/
├── ascend_provider.py
├── cache_provider.py
├── task_queue.py
├── performance.py
└── configs/
```

目录职责：

- `ascend_provider.py`：真实昇腾 NPU 生成实现，接收统一请求，调用图像生成模型；
- `cache_provider.py`：缓存模式实现，从已审核资产库读取结果；
- `task_queue.py`：生成任务队列、状态管理、失败重试；
- `performance.py`：生成耗时、显存/内存占用、成功率记录；
- `configs/`：采样步数、分辨率、LoRA 权重、模型版本等推理配置。

```text
models/generation/
├── sd15/
├── lora/
├── converted/
├── configs/
└── README.md
```

目录职责：

- `sd15/`：Stable Diffusion 基座模型权重；
- `lora/`：皮影风格 LoRA 权重或配置；
- `converted/`：昇腾转换后的模型文件；
- `configs/`：模型配置、推理参数、版本记录；
- `README.md`：模型来源、下载方式、启动方式、checksum、许可证说明。

```text
docs/
├── ascend-deploy.md
└── generation-report.md
```

目录职责：

- `ascend-deploy.md`：昇腾环境部署、模型转换、启动、故障排查；
- `generation-report.md`：生成速度、成功率、资源占用、失败案例和回退方案。

### A 主要交付物

- 可在昇腾 NPU 上运行的图像生成服务；
- 符合 `openapi.yaml` 的角色/场景贴图生成 API；
- Stable Diffusion + 皮影风格 LoRA 或等价风格控制方案；
- 任务队列、生成进度、缓存、失败重试；
- 模型版本、提示词、seed、参数记录；
- 无 NPU 时可用的缓存资产回退。

### A 不主责内容

- 不直接改前端舞台渲染逻辑；
- 不直接改手势识别接口；
- 不改变角色包格式、OpenAPI 字段或骨架命名，除非三人共同确认。

## 五、成员 B 职责：手势识别线

成员 B 负责摄像头输入、MediaPipe 手部关键点、手势分类、小模型训练、时序过滤、延迟和准确率指标。

### B 主责目录

```text
web/src/gesture/
├── GestureSource.ts
├── MediaPipeGestureSource.ts
├── MockGestureSource.ts
├── GestureClassifier.ts
├── TrajectoryRecognizer.ts
├── filters.ts
├── calibration.ts
├── features.ts
└── types.ts
```

目录职责：

- `GestureSource.ts`：手势模块统一接口。该文件属于公共冻结接口，B 按它实现；
- `MediaPipeGestureSource.ts`：真实摄像头手势源，封装 MediaPipe；
- `MockGestureSource.ts`：键盘/鼠标模拟手势。可由 C 先写，B 后续协助校准事件一致性；
- `GestureClassifier.ts`：静态手势分类，如张掌、握拳、捏合、指向；
- `TrajectoryRecognizer.ts`：动态手势识别，如上抬、挥动、快速前挥；
- `filters.ts`：One Euro Filter、滑动窗口、防抖、滞回、冷却；
- `calibration.ts`：个人手型、活动范围、阈值校准；
- `features.ts`：21 点关键点归一化、角度/距离特征提取；
- `types.ts`：手势内部类型，不能替代公共 schema。

```text
models/gesture/
├── mediapipe/
├── classifier/
├── labels.json
├── dataset/
├── metrics/
└── README.md
```

目录职责：

- `mediapipe/`：MediaPipe 模型文件或下载说明；
- `classifier/`：自训练 MLP、ONNX、TFJS 等小模型；
- `labels.json`：手势标签与动作名映射；
- `dataset/`：关键点数据、标注说明、小样本；
- `metrics/`：混淆矩阵、Macro-F1、P95 延迟、误触发报告；
- `README.md`：训练、导出、推理和评测说明。

```text
docs/
└── gesture-report.md
```

目录职责：

- 记录手势类别、采集方案、训练方法、测试集划分、准确率、延迟、误触发、弱光/遮挡表现。

### B 主要交付物

- 实时关键点演示页或调试面板；
- 实现 `GestureSource` 的真实手势源；
- 静态手势分类器和动态轨迹识别；
- 手势平滑、防抖、滞回、冷却和丢手恢复；
- 手势数据集、标签、训练代码或说明；
- Macro-F1、P95 延迟、误触发率等指标报告。

### B 不主责内容

- 不直接写 PixiJS 角色动画表现；
- 不决定最终视觉风格和动作夸张程度；
- 不改变 `GestureSource.ts` 的字段签名，除非三人共同确认。

## 六、成员 C 职责：舞台、控制、资产与总集成线

成员 C 负责前端主应用、数字皮影舞台、骨架、动作、资产处理、提示词模板、后端 API 骨架和集成演示。

### C 主责目录

```text
web/src/control/
├── InputRouter.ts
├── ContinuousMapper.ts
├── StateMachine.ts
└── ActionBindings.ts
```

目录职责：

- `InputRouter.ts`：把离散手势事件派发到动作状态机；
- `ContinuousMapper.ts`：把手掌位置、方向、速度、捏合强度映射为根节点位置和关节角；
- `StateMachine.ts`：裁决 `IDLE`、`WALK`、`JUMP`、`ATTACK`、`HIT` 等状态；
- `ActionBindings.ts`：绑定动作、音效、冷却时间和 UI 反馈。

```text
web/src/theater/
├── Stage.ts
├── Puppet.ts
├── MotionLibrary.ts
├── Effects.ts
└── AssetLoader.ts
```

目录职责：

- `Stage.ts`：创建 PixiJS Application，管理背景层、角色层、前景层、UI 层；
- `Puppet.ts`：读取 `rig.json`，装配 Container 骨骼树，执行 FK 关节变换；
- `MotionLibrary.ts`：读取 `assets/motions/*.json`，用 GSAP 或插值执行关键帧动作；
- `Effects.ts`：背光、透光、抖动、回弹、投影、幕布切换；
- `AssetLoader.ts`：角色包、背景、材质加载与缓存。

```text
web/src/ui/
├── TextureConfigPage.tsx
├── CalibrationPage.tsx
├── StagePage.tsx
├── AssetPicker.tsx
└── RecordingPanel.tsx
```

目录职责：

- `TextureConfigPage.tsx`：贴图配置页，选择角色类型、配色、服饰、纹样、场景；
- `CalibrationPage.tsx`：校准页，与 B 的校准逻辑对接；
- `StagePage.tsx`：自由操控主舞台；
- `AssetPicker.tsx`：角色、背景、道具即时切换；
- `RecordingPanel.tsx`：录制、回放、导出入口。

```text
web/src/assets/
├── assetClient.ts
├── assetCache.ts
└── validators.ts
```

目录职责：

- `assetClient.ts`：调用后端资产 API；
- `assetCache.ts`：浏览器端资产缓存；
- `validators.ts`：对角色包、rig、manifest 做前端校验。

```text
web/src/recording/
├── Recorder.ts
└── ReplayPlayer.ts
```

目录职责：

- `Recorder.ts`：封装 MediaRecorder 与动作事件记录；
- `ReplayPlayer.ts`：根据录制数据复现操控过程。

```text
server/app/api/
├── textures.py
├── tasks.py
├── assets.py
├── script.py
└── websocket.py
```

目录职责：

- `textures.py`：实现 `/api/textures/generate`；
- `tasks.py`：实现 `/api/tasks/{task_id}`；
- `assets.py`：实现 `/api/assets/{asset_id}`；
- `script.py`：可选增强，实现 `/api/script/generate`；
- `websocket.py`：实现 `/ws/generation/{sid}` 生成进度推送。

```text
server/app/postprocess/
└── pipeline.py
```

目录职责：

- `pipeline.py`：实现 `texture_to_assetpack`，完成抠图、对齐、模板遮罩切分、皮影化、导出角色包。

```text
server/app/assets/
├── repository.py
├── storage.py
└── manifest.py
```

目录职责：

- `repository.py`：资产索引和查询；
- `storage.py`：本地资产存储、路径管理；
- `manifest.py`：生成和校验资产清单。

```text
assets/
├── rigs/
├── motions/
├── textures/
├── demo/
├── characters/
└── backgrounds/
```

目录职责：

- `rigs/`：人形、动物形骨架模板；
- `motions/`：8 个基础动作关键帧；
- `textures/`：宣纸、皮革、背光、颗粒、镂空材质；
- `demo/`：答辩兜底角色包和背景；
- `characters/`：生成或缓存的角色资产包；
- `backgrounds/`：生成或缓存的背景资产。

```text
prompts/
├── character/
├── background/
├── negative.txt
└── README.md
```

目录职责：

- `character/`：角色正向提示词模板；
- `background/`：场景正向提示词模板；
- `negative.txt`：统一负向提示词；
- `README.md`：提示词参数、适用骨架、示例输出记录。

```text
controls/
└── controls.json
```

目录职责：

- 配置手势到角色动作、根节点、关节角、音效的映射；
- 例如 `palm_move -> root.position`、`point -> right_arm.angle`、`pinch -> attack`；
- C 主责维护，B 复核手势名称和识别稳定性。

### C 主要交付物

- 完整浏览器端应用；
- 皮影视觉规范、材质库、提示词模板；
- 2 套骨架、8 个动作、3 个以上背景；
- 角色包加载、切分、绑定和舞台渲染；
- 贴图配置页、校准页、自由操控页；
- 动作音效、即时切换、录制回放；
- Mock 纵向闭环和最终集成演示。

### C 不主责内容

- 不负责昇腾模型底层部署；
- 不负责手势模型训练细节；
- 不单方面修改公共接口。

## 七、公共冻结区

以下内容属于三人共同确认的接口和契约。第 1 天冻结后，任何修改都需要 A/B/C 三方确认，并同步更新前后端类型。

```text
packages/schemas/
├── gesture-event.schema.json
├── continuous-signal.schema.json
├── rig.schema.json
├── manifest.schema.json
├── script-act.schema.json
└── openapi.yaml
```

```text
web/src/gesture/GestureSource.ts
controls/controls.json
assets/rigs/*.json
assets/characters/*/rig.json
assets/characters/*/manifest.json
```

公共接口原则：

- `schemas/` 是全部数据契约的唯一事实来源；
- `openapi.yaml` 是前后端生成服务 API 的统一契约；
- `GestureSource.ts` 是 B 线接入 C 线的唯一入口；
- 角色包必须使用固定部件命名；
- 控制映射尽量放在 `controls.json`，不要把手势映射写死在代码里。

类型同步命令建议保留在根目录脚本中：

```bash
pnpm gen:types
```

同步目标：

- JSON Schema 生成 TypeScript 类型，供 `web/` 使用；
- JSON Schema 生成 Pydantic 模型，供 `server/` 使用。

## 八、核心接口说明

### 1. GestureSource

位置：

```text
web/src/gesture/GestureSource.ts
```

作用：

- B 线实现该接口；
- C 线通过该接口接收手势输入；
- 前端控制层不关心 B 使用 MediaPipe、MLP、规则分类器还是其他模型。

接口核心字段：

```typescript
export interface ContinuousSignal {
  present: boolean;
  palm: { x: number; y: number };
  direction: { x: number; y: number };
  velocity: { x: number; y: number };
  pinchStrength: number;
}

export interface GestureEvent {
  type: "gesture_action";
  action: "pinch" | "swipe_left" | "swipe_right" | "raise" | "fist" | "open_palm";
  confidence: number;
  hand: "left" | "right";
  timestamp: number;
}

export interface GestureSource {
  init(video: HTMLVideoElement): Promise<void>;
  onSignal(cb: (s: ContinuousSignal) => void): void;
  onGesture(cb: (e: GestureEvent) => void): void;
  destroy(): void;
}
```

### 2. 生成服务 API

位置：

```text
packages/schemas/openapi.yaml
```

主要端点：

```text
POST /api/textures/generate
GET  /api/tasks/{task_id}
GET  /api/assets/{asset_id}
POST /api/script/generate
WS   /ws/generation/{sid}
```

职责：

- 前端发起角色或场景贴图生成任务；
- 后端返回任务 ID；
- 前端轮询或 WebSocket 接收进度；
- 任务完成后读取资产包；
- 失败时可切换到缓存或 Mock 资产。

### 3. 角色包资产格式

位置：

```text
assets/characters/{character_id}/
```

标准结构：

```text
assets/characters/hero_rabbit/
├── preview.png
├── head.png
├── torso.png
├── upper_arm_l.png
├── lower_arm_l.png
├── upper_arm_r.png
├── lower_arm_r.png
├── leg_l.png
├── leg_r.png
├── prop.png
├── rig.json
└── manifest.json
```

职责：

- PNG 部件用于 PixiJS 渲染；
- `rig.json` 定义骨架层级、轴心、挂点、角度限制、层级顺序；
- `manifest.json` 记录提示词、seed、模型版本、生成时间、资产来源；
- A 生成贴图后，C 的后处理管线负责导出该格式；
- C 的前端舞台只加载该格式，不直接加载未经处理的大图。

## 九、主要模块在做什么

### `packages/schemas/`

这是项目的“宪法”。

它定义：

- 手势事件格式；
- 连续控制信号格式；
- 骨架 rig 格式；
- 资产 manifest 格式；
- 生成服务 API；
- 可选剧本增强格式。

任何模块之间传输的数据都应从这里派生类型，避免前后端字段不一致。

### `web/`

这是浏览器端主应用。

它负责：

- 用户配置贴图参数；
- 展示生成候选图；
- 调用后端生成和资产接口；
- 请求摄像头权限；
- 执行手势校准；
- 加载角色包和背景；
- 渲染 PixiJS 皮影舞台；
- 把连续手势映射到角色位置和关节；
- 把离散手势映射到跳跃、攻击、转身等动作；
- 录制和回放用户表演。

### `server/`

这是模型与资产服务。

它负责：

- 接收贴图生成请求；
- 分发到 mock、cache 或 ascend provider；
- 管理生成任务和进度；
- 提供资产读取接口；
- 执行或调用贴图后处理；
- 记录资产来源和生成参数；
- 为前端提供统一 API，隐藏底层生成模型差异。

### `assets/`

这是项目的离线和运行时资产库。

它负责：

- 保存骨架模板；
- 保存动作关键帧；
- 保存皮影材质；
- 保存 demo 角色包；
- 保存生成后的角色和背景；
- 支撑 Mock、缓存和答辩兜底。

### `models/`

这是模型权重和模型配置目录。

它负责：

- 保存 A 线图像生成模型权重和 LoRA；
- 保存 B 线手势识别模型和标签；
- 保存模型转换结果、评测结果和说明；
- 大模型权重通常不进 Git，只保留下载说明、版本号和 checksum。

注意：

- A 的模型代码放在 `server/app/generation/`；
- B 的推理代码放在 `web/src/gesture/`；
- `models/` 只放模型文件、配置、标签、评测和说明。

### `prompts/`

这是提示词模板库。

它负责：

- 维护角色提示词；
- 维护场景提示词；
- 维护统一负向提示词；
- 记录提示词适配的骨架类型；
- 记录稳定 seed 和示例结果。

### `controls/`

这是控制映射配置。

它负责：

- 把手势名称映射到角色动作；
- 把连续输入映射到角色根节点或关节；
- 定义阈值、限幅、冷却和优先级；
- 让控制逻辑可配置，而不是写死在代码里。

### `tests/`

这是质量验证目录。

建议包含：

- schema 校验测试；
- 角色包加载测试；
- rig 骨架合法性测试；
- 控制映射测试；
- 后端 API 集成测试；
- 手势事件回放测试；
- 舞台渲染和性能测试。

### `docs/`

这是交付文档目录。

建议包含：

- 昇腾部署说明；
- 手势训练与评测报告；
- API 和事件协议；
- 用户使用说明；
- 数据来源与授权清单；
- 用户测试和迭代记录；
- 答辩流程和故障回退方案。

## 十、三人协作规则

### 1. 谁主责谁合并

每个目录都指定主责人。其他成员可以提修改建议，但不要绕过主责人直接重构对方模块。

```text
A 主责：server/app/generation、models/generation、docs/ascend*
B 主责：web/src/gesture、models/gesture、docs/gesture*
C 主责：web/src/control、web/src/theater、web/src/ui、assets、prompts、server/app/api、server/app/postprocess
公共区：packages/schemas、openapi.yaml、GestureSource.ts、controls.json、rig/manifest 格式
```

### 2. 第 1 天必须冻结接口

冻结内容：

- OpenAPI 端点和字段；
- `GestureSource` 方法签名；
- 连续信号字段；
- 离散手势事件字段；
- 角色包目录结构；
- PNG 部件命名；
- `rig.json` 结构；
- `controls.json` 格式。

### 3. 第 2 天必须跑通纵向闭环

第一个闭环可以全部用 Mock：

```text
Mock 生成服务
  -> demo 角色包
  -> 前端加载角色
  -> MockGestureSource 输出手势
  -> 控制层映射
  -> PixiJS 舞台显示并移动角色
```

这个闭环跑通后，A 可以替换真实生成，B 可以替换真实手势，C 可以继续强化舞台表现。

### 4. 真实能力必须可回退

正式演示时至少保留三套兜底：

- 昇腾失败时使用 `PROVIDER=cache`；
- 缓存资产异常时使用 `assets/demo/`；
- 摄像头或手势失败时使用 `MockGestureSource` 的键盘/鼠标控制。

## 十一、建议第一批落地任务

### 公共任务

1. 初始化 monorepo；
2. 创建 `packages/schemas/`；
3. 创建 `openapi.yaml`；
4. 创建 `GestureSource.ts`；
5. 创建 `controls.json`；
6. 创建 demo 角色包结构；
7. 创建 `.gitignore`，排除模型大权重和生成缓存。

### A 第一批任务

1. 创建 `server/app/generation/provider.py` 抽象；
2. 创建 `mock_provider.py` 或 `cache_provider.py`；
3. 确认昇腾部署方式和模型路径；
4. 记录模型版本、依赖、启动命令；
5. 准备至少一套缓存生成结果。

### B 第一批任务

1. 跑通浏览器摄像头；
2. 跑通 MediaPipe Hand Landmarker 或 Gesture Recognizer；
3. 输出 `ContinuousSignal`；
4. 输出至少 5 类 `GestureEvent`；
5. 加入防抖、冷却和丢手恢复；
6. 准备键点数据采集和评测表。

### C 第一批任务

1. 搭建 React + TypeScript + PixiJS 前端；
2. 搭建 FastAPI API 骨架；
3. 制作人形和动物形 `rig.json` 模板；
4. 制作 8 个动作关键帧；
5. 制作 demo 角色包；
6. 实现角色加载、舞台显示、连续移动；
7. 实现贴图配置页、校准页、自由操控页的基本流程。

## 十二、最终交付物对照

软件交付：

- 浏览器端 AI 互动皮影戏应用；
- FastAPI 模型与资产服务；
- 昇腾部署配置；
- 手势识别模型和推理代码；
- 角色骨架、动作库、场景模板、材质库；
- 至少 2 个完整角色包和 3 个背景；
- Mock、缓存、在线推理三种模式。

文档交付：

- 项目需求与系统设计；
- API 和事件协议；
- 昇腾部署说明；
- 手势训练与性能报告；
- 数据来源与授权清单；
- 用户使用说明；
- 用户测试和迭代记录；
- 故障回退说明。

展示交付：

- 10 分钟答辩 PPT；
- 3 到 5 分钟完整演示视频；
- 现场可操作 Demo；
- 架构图、指标图、混淆矩阵和用户反馈。

## 十三、一句话分工总结

```text
A：让 AI 贴图生成服务在昇腾上跑起来，并保证有缓存回退。
B：让摄像头手势稳定变成统一的连续信号和离散事件。
C：把贴图、骨架、动作、舞台、UI 和接口集成为可玩的皮影戏台。
```

