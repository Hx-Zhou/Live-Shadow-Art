# Prompt Templates

Use these templates to keep generated outputs compatible with the asset postprocess pipeline.

- Character prompts should mention `articulated shadow puppet parts`, `clear separated limbs`, and the target rig type.
- Background prompts should avoid foreground characters and leave a readable central performance area.
- Save useful seeds and generated previews in asset manifests.
